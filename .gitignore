# Ferramentas de renderização (baixadas localmente, não versionar)
chrome/
node_modules/
package-lock.json

# Pré-visualizações geradas
build/preview/

# CSV exportado pela CLI (planilha de trabalho)
data/itens.csv
