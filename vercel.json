{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "echo 'sem etapa de build — site estático + funções /api'",
  "outputDirectory": ".",
  "cleanUrls": true,
  "functions": {
    "api/*.js": { "memory": 256, "maxDuration": 15 }
  }
}
